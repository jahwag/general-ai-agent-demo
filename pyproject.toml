[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "general-ai-agent-demo"
version = "0.1.0"
description = "Minimal Freshworks risk-tiered AI-agent capability demo"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]

[tool.setuptools]
packages = ["democtl"]
