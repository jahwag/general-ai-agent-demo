# Template only. Supply your own scoped credential and encrypt it before use.
# Copy to secrets.sops.yaml locally; that deployment file is ignored by Git.
vaults:
  github:
    GH_TOKEN: REPLACE_WITH_YOUR_SCOPED_GITHUB_TOKEN
